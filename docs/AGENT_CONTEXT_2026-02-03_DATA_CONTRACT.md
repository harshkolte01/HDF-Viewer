# AGENT CONTEXT — 2026-02-03 — /data Contract Frozen

- Updated `backend/docs/BACKEND.md` to add the frozen Phase 1 `/data` contract with three modes: `matrix`, `heatmap`, `line`.
- Defined common request params, response fields, mode-specific params, and error behavior.
- No backend code changes.
